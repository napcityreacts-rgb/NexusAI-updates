# NexusAI updates

This repo is the update feed for the NexusAI Android app. It holds release APKs and `version.json` only — no source code. The app checks `version.json` for new builds and installs them in-app.
